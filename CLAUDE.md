# Personal Knowledge Base

A single-user knowledge management tool inspired by NotebookLM. Upload documents and URLs, query them with Gemini FileSearch, and get cited answers.

## ⚠️ Current Working State (read this first)

- **Active branch**: `feature/tech-trax-crm-connector` (cut from `main`, no upstream yet). Building the **TechTrax CRM connector** as the second provider on the framework. `main` has Phase 5 + Phase 6 merged (orchestrator + Google Calendar) at `edf040d`.
- **Active environment**: **local Firebase emulator** (`firebase emulators:start --only functions,firestore,auth`) — port 5002 for functions, 4000 for the UI, project alias `demo-kb-local`. Staging (`the-knowledge-base-staging`) still runs the merged Phase 5+6 code from before this branch. Production untouched.
- **What's in flight (as of 2026-05-09 late EOD)**: TechTrax CRM connector with **15 tools** (the 7 lead tools, 7 pipeline-admin tools, plus `crm_list_users` for "assign to me" / by-name resolution). Plus framework-level enhancements that benefit *every* connector AND a now-verified-working orchestrator efficiency layer. **Latest session added (in chronological order)**: (1) **Validation-error coaching** — `tech_trax_crm/caseCorrect.ts` auto-corrects enum casing and coaches the agent on Joi-shape errors; (2) **`MAX_TOOL_TURNS` raised 4 → 100**; (3) **Tenant seed fix** — `seed-connector-test-user.js` now provisions `isFirstTimeSetup: false` + working hours; (4) **Pipeline-admin tools** for stages and custom fields, with `invalidateSchema(uid)` after every mutation; (5) **Cross-repo `localhost:3000/crm/leads` fix** — TechTrax FE `permissionStore` no longer hangs on backend error, TechTrax BE got a new `GET /api/crm/roles/my-permissions` route (declared before `/:id` so Express doesn't match the literal as an objectId); (6) **`ask_user` clarification flow + REQUIREMENTS GATHERING prompt** — agent batches all clarifying questions into one structured ask before any write; (7) **Self-verifying write handlers** — `verify(ok, message, details)` asserts the response body actually reflects the intended mutation (`assignedTo === resolvedAssignee`, `stageId === toStageId`, etc.); (8) **`crm_list_users` + "assign to me" shortcut** — JWT-decoded `getIdentity()` populates `isMe: true` on the connected user, `crm_assign_lead` accepts `"me"` and resolves before HITL; (9) **`limit=200` → `100`** on TechTrax `/api/platform/custom-fields` and `/api/platform/teams/users/*` (backend caps at 100); (10) **Orchestrator efficiency layer (history replay + write-aware stripping + Gemini context caching)** — agent now sees prior turns' `functionCall`/`functionResponse` parts (no more re-fetching what it already knows), reads stripped from history when later writes invalidate them, AND `client.caches.create({systemInstruction, tools, toolConfig, ttl: "3600s"})` per session with `name`/`hash`/`expiresAt` persisted to `sessionDoc.geminiCache`. **Verified end-to-end in emulator**: "assign Yahia to me" went straight to `crm_assign_lead` with zero redundant lookups (Layer 1 confirmed); session doc shows `geminiCache: { name: "cachedContents/...", hash: "...", expiresAt: ... }` populated and refreshed on every turn (Layer 3 confirmed). Visibility logs `[ORCH] cache created/refreshed: cachedContents/<id> (...)` now print on success so future audits don't need Firestore-spelunking. See "Phase 7" entry under Build Status for the full list.
- **How to start a fresh session continuing this work**:
  1. `cd /Users/Amr/Downloads/apps/the-knowledge-base && git checkout feature/tech-trax-crm-connector`.
  2. `cd functions && npm install && npm run build` (chrono-node, libphonenumber-js, googleapis, etc. all already in package.json).
  3. **Start the TechTrax backend** (it's at `/Users/Amr/Downloads/Tech Trax/inbox-repo/techtrax-backend`): `cd "/Users/Amr/Downloads/Tech Trax/inbox-repo/techtrax-backend" && npm run dev`. Listens on `:5001`. Connects to remote Mongo at 34.30.26.207 via TLS — credentials in its `.env`.
  4. **Start the TechTrax FE** (so you can showcase both surfaces during demo): `cd "/Users/Amr/Downloads/Tech Trax/inbox-repo/techtrax-frontend-revamped" && npm run dev`. Listens on `:3000`. Login: `connector-test@techtrax.io` / `P@ssw0rd!`. If pushed to `/crm/setup-wizard`, run `localStorage.clear(); location.reload()` in DevTools console (the FE has a localStorage-persisted setup flag that can go stale).
  5. **If the seeded tenant is missing or wrong** (e.g. wizard keeps appearing or no leads): `cd "/Users/Amr/Downloads/Tech Trax/inbox-repo/techtrax-backend" && node scripts/seed-connector-test-user.js`. Idempotent — preserves existing user/tenant if they exist, just patches the setup flag + working hours.
  6. **Start Firebase emulators**: `export JAVA_HOME=/opt/homebrew/opt/openjdk && export PATH=$JAVA_HOME/bin:$PATH && firebase emulators:start --project demo-kb-local --only auth,firestore,functions,storage`. Functions on `:5002`, Emulator UI on `:4000`. Watch stdout for `[CONFIRM]`, `[CONNECTOR]`, `[tech_trax_crm.*]` lines.
  7. **Start the KB frontend**: `npm run dev` (port 5173 — NOT staging mode locally). Verify `src/lib/firebase.ts` wires `connectFunctionsEmulator` / `connectFirestoreEmulator` / `connectAuthEmulator` when running locally.
  8. **Login to KB**: `me@test.com` / `Test1234!` (Auth Emulator UI at http://localhost:4000/auth — create if absent). For staging KB use `admin@test.local` / `Admin@123`.
  9. **Connect Tech Trax CRM in KB**: Settings → Connectors → Tech Trax CRM → Connect. Use Base URL `http://localhost:5001`, email `connector-test@techtrax.io`, password `P@ssw0rd!`.
  10. Submit chat prompts to exercise the CRM. The agent will route to `crm_*` tools. Both the TechTrax FE (`:3000`) and KB chat (`:5173`) read/write the same lead set, so you can demo bidirectional visibility.
- **Browser control**: Chrome DevTools MCP is registered at user scope (`~/.claude.json`) — after restart, you have tools to drive Chrome directly (clear localStorage, reload, click, screenshot, network inspect). Use it instead of asking the user to relay devtools output.
- **How to deploy to staging or prod**: don't from this branch. When TechTrax is verified, merge into `main` and deploy via `firebase deploy --only functions --project staging` first, then prod after env vars are set on the prod GCP project.
- **Feature flags (4 layers — Phase 7 added one)**:
  1. `MULTI_TOOL_ENABLED` (functions env) — gates the orchestrator path. `true` on staging, `false` on prod.
  2. `CONNECTORS_ENABLED` (functions env) — gates `bootConnectors()` and per-user connector tool merging. `true` on staging, `false` on prod. **Must remain `false` wherever any provider's required env vars (`GOOGLE_OAUTH_*`, `TECH_TRAX_*` if any) are unset.**
  3. `VITE_CONNECTORS_ENABLED` (frontend env, build-time) — gates `/settings/connectors` route and Connectors nav button. `true` on staging, `false` on prod.
  4. **TechTrax credentials are per-user, captured by the `connectTechTraxCrm` callable**, not a global env var. Stored at `users/{uid}/connectors/tech_trax_crm/_secrets` (encrypted via Cloud KMS envelope, same shape as Google's refresh token).

## Resuming work in a new session — quick orientation

If you're a fresh Claude session reading this, here's what you need to know to continue without breaking anything:

### Where we are right now
- Branch `feature/tech-trax-crm-connector`, **uncommitted**. The current diff = full TechTrax CRM connector + Phase 7 framework enhancements + the latest session's coaching/cap/seed fixes. Run `git status` and `git diff main..HEAD -- ':(exclude)package-lock.json'` to see the full picture.
- `main` has Phase 5 + 6 merged at `edf040d`. **Don't push this branch to main yet** — TechTrax connector hasn't been verified end-to-end through every flow.
- Local Firebase emulator is the test environment. Staging Calendar still works (deployed) but TechTrax CRM is local-only until merged.
- **Demo readiness**: the connector handles enum-casing typos automatically, coaches the agent on Joi validation errors so users see friendly responses, and the CRM FE runs side-by-side with KB chat at `:3000` for live demos.

### Files to skim first
- `functions/src/services/connectors/types.ts` — the `ConnectorProvider`, `ConnectorTool`, `ConnectorContext` shape. Every provider plugs in here.
- `functions/src/services/connectors/registry.ts` — the singleton registry + `dispatch()` (read vs write vs scope_required vs validation_pending vs awaiting_approval result kinds).
- `functions/src/services/connectors/google_calendar/index.ts` — the reference provider.
- `functions/src/services/connectors/tech_trax_crm/{index,handlers,validator,schemaCache,client,phone,caseCorrect,connectCallable,credentialsForm,declarations}.ts` — current TechTrax work. `caseCorrect.ts` is the validation-error coaching layer (auto-fix enum casing + coach on Joi errors); wraps every write request in `requestWithEnumRetry`.
- `functions/src/services/orchestrator.ts` — Gemini orchestrator. `MAX_TOOL_TURNS=100` (raised from 4 in last session); graceful cap-hit token so UI never goes blank.
- `functions/src/services/connectors/dateTime.ts` — natural-language date parsing helper.
- `functions/src/index.ts` — Cloud Functions exports (`chat`, `connectorOAuth*`, `getConnectorStatus`, `disconnectConnector`, `confirmPendingAction`, `cancelPendingAction`, `getPendingActionStatus`, `connectTechTraxCrm`, `techTraxCredentialsForm`).
- `src/lib/connectorActionStore.ts` — module-level runtime state for HITL cards.
- `src/features/chat/components/ActionApprovalCard.tsx` — the HITL approval UI.

### Patterns to follow when adding new tools / connectors
- **Never emit `undefined`** in objects you persist to Firestore. Build incrementally (`if (raw.x) obj.x = raw.x`).
- **Stripping `undefined`** with `Object.entries` recursion is the wrong default — it clobbers `Timestamp` and other class instances (`constructor !== Object`). The helpers in `audit.ts` / `pendingActions.ts` already special-case this; if you write your own, use the same constructor check.
- **Idempotency on writes** — derive a deterministic key from `{uid, sessionId, tool, canonical(args)}` and pass it to the upstream API (Calendar accepts `id`; Tech Trax accepts `Idempotency-Key`).
- **Read-before-write** — for update / delete / RSVP / transition, always GET first to validate existence and (for Tech Trax) capture a fresh `Last-Modified` lock token.
- **Throw normalized errors** — registry catches `{code, message, retryable}` shape and writes `reasonCode` to the audit log. Don't throw raw `Error` from handlers; throw the normalized object.
- **Preflight validation** for write tools — implement `preflight(args, ctx)` on the `ConnectorTool`. Throw `ValidationPendingError(missing, message)` so the agent re-asks the user *before* the HITL approval card is shown. This is what makes phone normalization, date normalization, and missing-required-field errors a great UX.
- **Mutate args in preflight** when normalizing inputs (phone, dates) — the registry computes idempotency from args BEFORE preflight, so post-preflight mutations don't change the key. The HITL card preview reads from the (now-canonical) args.

### Things to re-verify before declaring something "done"
1. `cd functions && npm run build` (TypeScript strict).
2. Run all connector tests: `cd functions/lib/services/connectors && for t in __tests__/*.test.js tech_trax_crm/__tests__/*.test.js; do echo "=== $t ==="; node "$t" || break; done`. Covers `stateJwt`, `crypto`, `registry`, `dateTime`, `phone`, and `caseCorrect`.
3. Functions emulator restarts cleanly on `lib/` change; `[CONFIRM]` / `[CONNECTOR]` lines appear in stdout when chat triggers a tool.
4. Approval card persists state across browser refresh (open chat with a resolved card, ⌘⇧R, card should stay collapsed at "done ✓" not bounce back to pending).
5. CONNECTORS_ENABLED=false path leaves orchestrator declarations identical to baseline (regression check).

### What the user is likely to ask next
- Continue building TechTrax CRM tools or fix issues that surface during testing.
- Add a third connector (Gmail / Drive / Notion / Linear were mentioned in roadmap).
- Polish the agent's tool-selection prompt so it picks the right tool more reliably across many providers.
- Eventually merge this branch into `main` once TechTrax flows are verified.

## Tech Stack

- **Frontend**: React + Vite + TypeScript, shadcn/ui + prompt-kit + Tailwind CSS
- **Fonts**: Outfit (headings, `font-heading`), Urbanist (body, `font-body`)
- **Auth**: Firebase Auth (email/password, signup disabled on frontend)
- **Database**: Cloud Firestore
- **Storage**: Cloud Storage
- **Backend**: Cloud Functions (Node.js, TypeScript) in `functions/`
- **RAG**: Gemini FileSearch Store API (single store, `uploadToFileSearchStore` with customMetadata)
- **URL Extraction**: Jina Reader API
- **Hosting**: Netlify

## Project Structure

- `src/` — React frontend
  - `src/features/` — Feature modules (auth, notebooks, sources, chat, settings)
  - `src/components/ui/` — shadcn/ui + prompt-kit components
  - `src/lib/firebase.ts` — Firebase SDK initialization
  - `src/lib/utils.ts` — shadcn utility (cn function) + RTL text detection (`getTextDir`)
  - `src/lib/firestore.ts` — Typed Firestore collection/document helpers
  - `src/lib/formatters.ts` — Date, file size, text truncation formatters
  - `src/lib/api.ts` — Cloud Functions callable helper
  - `src/lib/streaming.ts` — SSE streaming client for chat endpoint
  - `src/types/` — Shared TypeScript interfaces (Notebook, Source, Session, Message)
  - `src/config/constants.ts` — File types, size limits, status config, Gemini models, function URLs
- `functions/src/` — Cloud Functions backend
  - `functions/src/services/` — API wrappers (Gemini, Jina, Storage)
  - `functions/src/middleware/` — Auth token validation
  - `functions/src/config.ts` — Environment variable access
  - `functions/src/types/` — Backend type definitions
- `functions/src/telegram/` — Telegram bot integration
  - `webhook.ts` — Main onRequest handler with webhook secret validation
  - `commands.ts` — Command handlers (/start, /notebooks, /switch, /model, /status, /reset, /help, /unlink) + web tool commands in /help
  - `chat.ts` — Chat message handler with session lifecycle (24h expiry) + slash command parsing (/web, /maps, /url)
  - `telegramClient.ts` — Telegram Bot API wrapper (sendMessage, markdownToTelegramHtml)
  - `rateLimiter.ts` — In-memory sliding window rate limiter (5/min per user, 30/min global)
  - `types.ts` — Telegram-specific type definitions
- `prompt-templates/` — Reusable system prompt templates (future feature)
- `firestore.rules` — Firestore security rules
- `storage.rules` — Cloud Storage security rules

## Key Commands

```bash
npm run dev          # Start Vite dev server (port 5173)
npm run build        # Build for production
cd functions && npm run build  # Build Cloud Functions
firebase deploy --only functions  # Deploy functions
firebase deploy --only firestore:rules  # Deploy Firestore rules
firebase deploy --only storage  # Deploy storage rules
firebase deploy --only firestore:rules,firestore:indexes  # Deploy rules + indexes together
```

## Environment

- Firebase projects (aliases in `.firebaserc`):
  - `prod` (default): `the-knowledge-base-82d72` — main user-facing project
  - `staging`: `the-knowledge-base-staging` — for testing the multi-tool orchestrator and other risky changes before merging to `main`
- Staging Storage bucket is `kb-staging-files` (custom name; the default `*.firebasestorage.app` and `*.appspot.com` were domain-locked). Mapped via `firebase target:apply storage staging kb-staging-files`. Frontend reads `VITE_FIREBASE_STORAGE_BUCKET` so this is transparent.
- Staging FileSearch store: `fileSearchStores/kbstaging-v5jcj3myvved` (separate from prod store). Created via the Gemini API.
- Staging reuses the prod Gemini API key + Jina API key (single-user app, low volume). Telegram is NOT deployed in staging.
- `MULTI_TOOL_ENABLED=true` is set ONLY in `functions/.env.the-knowledge-base-staging` — prod stays on legacy single-tool path until verified.
- Frontend env files: `.env` (prod), `.env.staging` (staging) — Vite picks via `--mode`. Run `npm run dev:staging` or `npm run build:staging` to target staging.
- Functions env: `.env` (prod), `.env.the-knowledge-base-staging` (auto-loaded by Firebase CLI when deploying to that project). Do NOT use deprecated `functions.config()`.
- Deploy commands: `firebase deploy --only functions --project staging` (or `npm run deploy:staging`). For prod use `--project prod` explicitly.
- Frontend vars use `VITE_` prefix. Netlify env vars (Project config → Environment variables) are needed for prod hosting builds only.
- Branch convention: risky/exploratory work (e.g. `feat/multi-tool-orchestrator`) lives on a feature branch, deploys to `staging` first, only merges to `main` after verification.

## Conventions

- TypeScript strict mode
- Functional React components with hooks
- Feature-based folder structure
- shadcn/ui for all base components
- prompt-kit for chat UI components
- `@/` path alias maps to `src/` (configured in tsconfig)
- shadcn components use base-ui primitives (not radix) — Dialog uses `@base-ui/react/dialog`
- Firestore `list` queries: avoid `resource.data` checks in security rules for collection queries
- Firestore composite indexes: define in `firestore.indexes.json`, single-field indexes are auto-created
- Cloud Functions use v2 API (`firebase-functions/https`, `firebase-functions/firestore`)
- Gemini SDK: use `@google/genai` (not `@google/generative-ai` which lacks Files API)
- Upload pipeline: frontend uploads to Cloud Storage → Firestore trigger → `uploadToFileSearchStore` (single-step store upload with metadata)
- Chat streaming: SSE via Cloud Functions v2 `onRequest` (not `onCall` — it doesn't support streaming)
- Gemini FileSearch `metadataFilter` is active — uses AIP-160 filter syntax (`notebook_id = "<id>"`) for notebook-level data isolation
- CRITICAL: Gemini metadataFilter does NOT support camelCase keys (silently returns no results) — always use snake_case for custom metadata keys
- Source tags from users should also use snake_case keys to be filterable
- Gemini model IDs: use preview/stable strings (e.g. `gemini-3-flash-preview`, `gemini-2.5-flash`) — check for deprecations
- Source tags: stored as `customMetadata` on Gemini store documents alongside `notebookId`
- RTL support: `getTextDir()` in `src/lib/utils.ts` auto-detects Arabic/Hebrew text and sets `dir="rtl"` on message content
- Chat layout: user messages are right-aligned dark bubbles, AI messages are left-aligned with bot icon — conversational style
- Per-notebook system prompt: stored as `systemPrompt` field on notebook doc, appended to default system prompt server-side
- Token counting: server-side via Gemini `usageMetadata.totalTokenCount`, stored on session via Firestore `increment()`
- Summarization: triggers at 500K tokens, uses `gemini-2.5-flash`, 60s cooldown on failure
- Channel-aware prompts: `CHANNEL_PROMPT_OVERRIDES` in config.ts appends channel-specific instructions (e.g. no citations for Telegram)
- Telegram bot: uses raw `fetch` calls to Telegram Bot API (no npm package), webhook via Cloud Functions v2 `onRequest`
- Telegram auth: email OTP linking via Firebase Trigger Email extension + Gmail SMTP, stored in `telegramLinks` collection
- Telegram formatting: Gemini Markdown → HTML conversion (`markdownToTelegramHtml`) with `"HTML"` parse mode
- Telegram sessions: 24h auto-expiry, in-memory `chatStates` Map (rehydrated on cold start from Firestore)
- Cloud Functions v2 requires `allUsers` invoker policy for public webhooks: `gcloud functions add-invoker-policy-binding <name> --member=allUsers`
- Gemini built-in tools (FileSearch, googleSearch, urlContext, googleMaps) CANNOT be combined directly in the same request — official FileSearch docs explicitly forbid it. Multi-tool support is achieved via the orchestrator (`functions/src/services/orchestrator.ts`): parent Gemini 3 model runs FileSearch + custom function declarations (`web_search`, `maps_search`, `url_fetch`) with `includeServerSideToolInvocations: true`. Each function maps to a single-purpose sub-agent in `functions/src/services/subagents/` that runs ONE built-in tool on `gemini-3.1-flash-lite`. Lightweight QC validates each sub-agent result before it's exposed to the parent (soft mode: accepts non-empty summary even without grounding citations). Gated by `MULTI_TOOL_ENABLED` env flag; falls back to legacy single-tool path on Gemini 2.5 or when a slash command is used
- Streaming part-merge for Gemini 3 thinking models (`functions/src/services/orchestrator.ts`): parts from each chunk are pushed in order, and any signature-only part (no data field) is folded into the most recent data-bearing part. This preserves `thoughtSignature` while keeping mutually-exclusive `oneof` data fields (text/functionCall/toolCall/toolResponse) on separate parts — the API rejects parts with multiple data fields set
- URL prefetch via Jina Reader (`functions/src/services/urlPrefetch.ts`): when the user pastes URLs in their chat message, the orchestrator AND the legacy path call Jina to extract clean markdown (capped: 3 URLs/turn, 8000 chars each) and inject the content into the user message before sending to Gemini. Failures are logged and skipped — `url_fetch` sub-agent is still available as a fallback. Independent of `MULTI_TOOL_ENABLED` so prod gets URL prefetch too once merged
- Tool call surfacing in the UI (`src/features/chat/components/ToolCallCard.tsx`): backend emits a new `tool_call` SSE event (`{id, name, args, status: running|done|error, output?, citations?, error?, durationMs?}`) for each Jina prefetch and each sub-agent dispatch. Frontend collects them into `streamingToolCalls` (in `useChat.ts`) and renders inline pills above the assistant message — click to expand the raw tool output. Persisted on the assistant message doc as `toolCalls?: ToolCall[]` so they survive reload. Tool-call events flow through `streaming.ts` via a new `onToolCall` callback. Server-side `toolCall`/`toolResponse` parts (Gemini's native FileSearch invocation) are NOT surfaced as cards in v1 — citations on the message already indicate FileSearch was used
- Soft QC mode (`functions/src/services/subagents/qc.ts`): sub-agent results are accepted as long as `summary.trim()` is non-empty. Zero-grounding-citation results are logged (`[QC <name>] soft-pass`) and passed through, not rejected. Tighten later if hallucinations become a problem
- Markdown link styling (`src/components/ui/markdown.tsx`): `<a>` tags rendered by ReactMarkdown use the standard blue underline (`text-blue-600 underline`), open in new tab with `noopener noreferrer`, and `break-all` so long URLs wrap inside the chat bubble
- Web tools via slash commands: `/web` (Google Search), `/maps` (Google Maps), `/url` (URL Context) — parsed in frontend (`useChat.ts`) and Telegram (`chat.ts`), sent as `toolOverride` param to backend
- Default tool is always FileSearch (uploaded sources); slash commands override to a specific web tool for that message only
- Per-notebook tool toggles stored as `tools` field on notebook doc (e.g. `{ googleSearch: true }`) — controls which tools are available but slash commands are the actual trigger
- Frontend slash command menu: typing `/` in chat input shows a popup menu (ChatInput.tsx) with keyboard navigation
- Multimodal chat: users can attach images, audio, and PDFs in chat messages (web + Telegram)
- Chat attachments stored in Cloud Storage at `users/{uid}/chat-attachments/{notebookId}/{sessionId}/`
- Attachment metadata stored on message doc as `attachments?: Attachment[]` (type, mimeType, fileName, sizeBytes, storageRef, downloadUrl)
- Gemini multimodal: attachments sent as `inlineData` parts alongside text in the user message; history remains text-only
- Chat function memory bumped to 512MiB to handle file download + base64 encoding
- Telegram media: photos, voice messages, audio files, and PDF documents → downloaded via Bot API → uploaded to Cloud Storage → passed to Gemini
- Max chat attachment size: 10MB per file, max 5 attachments per message

## Connectors — deploy + KMS commands cheat sheet

```bash
# (One-time per env, already done for staging)
gcloud config set project the-knowledge-base-staging
gcloud services enable cloudkms.googleapis.com calendar-json.googleapis.com
gcloud kms keyrings create connectors --location=us-central1
gcloud kms keys create refresh-tokens \
  --keyring=connectors --location=us-central1 --purpose=encryption
gcloud kms keys add-iam-policy-binding refresh-tokens \
  --keyring=connectors --location=us-central1 \
  --member=serviceAccount:$(gcloud projects describe the-knowledge-base-staging --format='value(projectNumber)')-compute@developer.gserviceaccount.com \
  --role=roles/cloudkms.cryptoKeyEncrypterDecrypter
openssl rand -base64 48   # → CONNECTOR_STATE_SIGNING_SECRET

# Deploy
firebase deploy --only firestore:rules --project staging
firebase deploy --only functions --project staging

# Frontend
npm run build:staging   # then your usual Netlify deploy

# Sanity check after deploy
gcloud functions list --project=the-knowledge-base-staging --regions=us-central1 \
  | grep -E 'connector|chat|getConnector|disconnect|confirmPending|cancelPending'
# Expect: connectorOAuthStart, connectorOAuthCallback, getConnectorStatus,
#         disconnectConnector, confirmPendingAction, cancelPendingAction (plus existing ones)
```

## Build Status

- **Phase 0**: Complete (scaffold)
- **Phase 1A**: Complete (auth, notebooks, file upload, source status, security rules)
- **Phase 1B**: Complete (URL ingestion, chat with streaming/citations, model selection, session reset, upload dialog with tags)
- **Phase 1C**: Complete (summarization, archive, system status/warm-up, per-notebook system prompt, token counting, RTL support, chat layout redesign, Netlify deployment)
- **Phase 2**: Complete (Telegram bot — email OTP linking, notebook/model selection, session management, rate limiting, channel-aware prompts, location support, HTML formatting)
- **Phase 3**: Complete (Web tools — Google Search, Google Maps, URL Context via slash commands /web /maps /url, per-notebook tool settings, slash command menu in web UI)
- **Phase 4**: Complete (Multimodal chat — image/audio/PDF attachments in web and Telegram, Gemini native vision/audio understanding)
- **Phase 5** (in-progress on `feat/multi-tool-orchestrator`, deployed to staging only): Multi-tool orchestrator. Parent Gemini 3 model coordinates FileSearch + 3 single-purpose sub-agents (`web_search`, `maps_search`, `url_fetch`) running on `gemini-3.1-flash-lite` via custom function declarations + `includeServerSideToolInvocations: true`. Lightweight QC, Jina URL prefetch as a pre-step, tool-call cards in the UI, blue-link styling. Gated by `MULTI_TOOL_ENABLED` env flag (`true` on staging, off on prod). Falls back to legacy single-tool path on Gemini 2.5 or when a slash command is used. Not yet merged to `main`/prod
- **Phase 6** (merged to `main`, builds on Phase 5): Connectors framework + Google Calendar. Generic provider registry, OAuth (HS256 state JWT, 10-min TTL), Cloud KMS envelope encryption for refresh tokens (AES-256-GCM with per-record DEK wrapped by KMS KEK), HITL approval gate for write actions (`pendingActions` collection + `action_approval_required` SSE event + `ActionApprovalCard` UI + `confirmPendingAction`/`cancelPendingAction` callables), per-tool audit log at `auditLogs/{id}` (90-day TTL), incremental scope authorization (read scopes at first connect → `scope_expansion_required` SSE → `mode=expand` OAuth flow → write scope on demand). Calendar tools: `gcal_freebusy`, `gcal_list_events` (read), `gcal_create_event`, `gcal_update_event`, `gcal_delete_event`, `gcal_respond_to_event` (write — HITL gated, idempotent via SHA-256 args hash, read-before-write on update/delete/RSVP). Gated by `CONNECTORS_ENABLED` (functions) and `VITE_CONNECTORS_ENABLED` (frontend). Backwards-compatible: when no connectors registered or none connected, orchestrator behavior is identical to Phase 5. KMS provisioned on staging; OAuth client + consent screen complete; E2E verified (read tools, write tools with HITL, scope expansion, mixed FileSearch + Calendar). **Merge commit**: `edf040d`.

### Phase 6 — non-obvious things learned the hard way

- **Firestore rejects `undefined`** — Calendar API responses contain optional fields (`description`, `location`) that come back undefined. Persisting them via `Timestamp.fromMillis(...)` plus result objects requires stripping `undefined` before write. `audit.ts` and `pendingActions.ts` both have a `stripUndefined` helper. CRITICAL: only recurse into plain objects (`constructor === Object`); preserve class instances like `Timestamp` — otherwise `Timestamp.toMillis()` is destroyed by re-cloning.
- **HITL state machine** — `confirmPendingAction` does NOT mark the action `approved` before executing. Goes straight from `awaiting_approval` → `executed` (or `error`) atomically inside `executeApprovedAction`. The intermediate `approved` state was redundant and broke the executor's status check.
- **Card identity across the streaming → persisted boundary** — when `streaming=false`, `StreamingMessage` unmounts and `ChatMessage` re-mounts the same card from `message.pendingActions`. Without a shared store, the card's resolution state (Confirmed / Cancelled / etc.) would reset and the UI would lie. Solved by `src/lib/connectorActionStore.ts` — module-level Map of resolutions keyed by `actionId`, subscribed via `useSyncExternalStore`. Survives unmount/remount within the same SPA session; resets only on full page reload (which is acceptable — backend `auditLogs` is the source of truth for what actually happened).
- **Never fake "executed"** — earlier iteration defaulted historical-prop cards to `state="executed"` when no runtime entry existed, which made the UI claim a confirmation that never happened. Fix: cards always derive state from runtime store OR fall back to the action's own lifecycle (`pending` if `expiresAt > now`, otherwise `expired`). No spoofed states.
- **Stale cards after page reload** — actionIds older than 5 minutes are server-side-expired. Clicking Confirm on yesterday's card returns `failed-precondition` / `deadline-exceeded`. Expected behavior; UI surfaces the error.
- **`stripUndefined` and Firestore Timestamps** — see first bullet. Generic `Object.entries(...)` recursion clobbers `Timestamp` to `{seconds, nanoseconds}` without `.toMillis()`. The constructor check is load-bearing.
- **Telegram bot signature** — `routeChat` now takes `uid` + `sessionId`. `functions/src/telegram/chat.ts` was updated to pass `link.firebaseUid` + the session id; legacy single-tool path doesn't use them, but the signature must compile.

- **Phase 7** (in-progress on `feature/tech-trax-crm-connector`, builds on `main`): TechTrax CRM connector + framework-level UX hardening. Per-user credential capture via `connectTechTraxCrm` callable (no global OAuth — TechTrax uses bearer tokens minted from a per-user email/password sign-in, encrypted via the same KMS envelope). **14 tools** (the original 7 lead tools + 7 pipeline-admin tools added 2026-05-09 so the agent can configure the CRM, not just operate on it): `crm_list_stages`, `crm_list_leads`, `crm_get_lead`, `crm_list_custom_fields` (read), `crm_create_lead`, `crm_update_lead`, `crm_transition_stage`, `crm_assign_lead`, `crm_create_stage`, `crm_update_stage`, `crm_delete_stage`, `crm_create_custom_field`, `crm_update_custom_field`, `crm_delete_custom_field` (write — HITL gated). Schema cache at `users/{uid}/connectors/tech_trax_crm/_schema/v1` (1-hour TTL) maps custom-field names → fieldIds for routing agent inputs. Joi-aware preflight validation (`validateCreate`, `validateTransition`) surfaces missing mandatory fields as `ValidationPendingError` so the agent re-asks the user *before* the HITL approval card is shown. Optimistic locking via `If-Unmodified-Since` headers; the connector refreshes the lock token at handler start (squashes the user-think-time race) and re-captures `Last-Modified` between chained writes (PATCH then transition). Same KMS envelope encryption + audit log + HITL framework as Phase 6. Backwards-compatible: when `tech_trax_crm` provider isn't registered (no `bootConnectors()` call) or the user hasn't connected, behavior unchanged.

### Phase 7 — framework-level enhancements (apply to all connectors)

These benefit every provider — Google Calendar, TechTrax CRM, and any future ones:

- **Natural-language date parsing** (`functions/src/services/connectors/dateTime.ts`). Built on `chrono-node`. `parseDateTimeToIso(input, opts)` accepts ISO 8601 strings (pass-through), epoch ms (number), native `Date`, or natural language ("tomorrow at 4pm", "next Monday 10am", "in 2 weeks"). Default timezone `Africa/Cairo` — override per call. `dateOnly: true` for fields like `DOB` returns `YYYY-MM-DD`. Used in:
  - `google_calendar/handlers.ts` — `start.dateTime`, `end.dateTime`, `timeMin`, `timeMax`.
  - `tech_trax_crm/handlers.ts` — known date-shaped fields (`bookingDate`, `snoozeDate`, `followUpDate`, `appointmentDate`, `meetingDate` for datetime; `DOB`, `dob`, `birthDate` for date-only).
- **Phone normalization** (`functions/src/services/connectors/tech_trax_crm/phone.ts`). Built on `libphonenumber-js`. `normalizePhoneToE164(input)` parses any free-form phone string to E.164 (`+201012345678`). Default country `EG` (override per tenant later). Local Egyptian numbers like `01012345678` or `1012345678` get auto-prefixed to `+20`; international numbers (`+1 415 555 0100`) are preserved. Junk throws so the agent can re-ask. Wired into create/update lead handlers and the preflight (so the approval card preview shows the canonical number).
- **Optimistic-lock token refresh** (`tech_trax_crm/handlers.ts:refreshLockToken`). HITL approval window means preflight `Last-Modified` is 30s+ old by Confirm time → 412 `precondition_failed`. Handler refreshes the lock token via a fresh `GET /api/crm/leads/{id}` at execute time, and captures the new `Last-Modified` from each write response so chained writes don't race against themselves.
- **Persisted action state recovery on refresh** — new `getPendingActionStatus({actionId})` callable in `functions/src/index.ts`. Auth-gated, ownership-checked, read-only (never mutates the doc). Returns `{status, result, error, expiresAt}`. The frontend `ActionApprovalCard` calls it on mount when the in-memory `connectorActionStore` is empty (typical after a page refresh) and seeds the runtime state — so cards keep their resolution (`executed` / `cancelled` / `error` / `expired`) instead of falsely showing a still-clickable Confirm button.
- **Enriched 4xx error messages** (`tech_trax_crm/client.ts:enrichClientErrorMessage`). When the CRM backend returns `400 { message: "Validation error", errors: [...] }`, the helper appends the per-field `errors` / `details` / `validationErrors` reasons so the agent (and audit log, and UI) gets `"Validation error — phone: must be in international format"` instead of just `"Validation error"`.
- **`confirmPendingAction` no longer eats errors** — earlier the catch block was `err instanceof Error ? err.message : "Action execution failed."` which silently dropped registry-thrown plain-object errors (`{code, message, retryable}` shape). Now extracts `.message` from object form too AND `console.error`s the full err so emulator stdout shows `[CONFIRM] action=... tool=... failed: <real reason> { ... }`.
- **`schemaCache.normalizeStage` never emits `undefined`** — earlier it built `{color: undefined, allowedNextStages: undefined, slaHours: undefined}` for stages with missing optional fields, which Firestore rejected when caching. Now it only writes keys with defined values. Pattern to follow for any handler that returns objects to be stored: build incrementally with `if (raw.x) obj.x = raw.x` rather than always emitting `undefined`. The `stripUndefined` helpers in `audit.ts` and `pendingActions.ts` are a safety net but cleaner to never produce `undefined` in the first place.
- **`handleTransitionStage` skips empty PATCH** — when the agent passes `fieldUpdates` whose keys all route to `unrouted` (schema cache didn't recognize them), the resulting patchBody is `{}`. Backend Joi rejects with `"value must have at least 1 key"`. Handler now skips the PATCH entirely in that case, returns `fieldsDropped` in the response so the agent can re-ask, and proceeds with the transition (which then independently surfaces any real mandatoryFields gaps).
- **Chrome DevTools MCP installed at user scope** (`~/.claude.json`). Command: `npx -y chrome-devtools-mcp@latest`. Lets future sessions drive a live Chrome browser directly — useful for the demo flow (clear localStorage on the CRM tab, reload, screenshot the dashboard, inspect network for the KB chat → connector dispatch path) without needing the user to relay devtools output. Restart Claude Code for the tools to register.
- **TechTrax tenant seed now provisions setup-complete + working hours.** `seed-connector-test-user.js` was leaving `isFirstTimeSetup: true` (default) and `workingHours: []`, causing the CRM FE's `SetupGuard` to bounce every login to `/crm/setup-wizard`. Updated to set `isFirstTimeSetup: false` and seed Mon–Fri 9–5 working hours on both create and reuse paths. Also fixed the tenant lookup: `findOne({tag})` always returned null (Tenant model has no `tag` field), so each seed run was minting a fresh tenant; now we prefer the existing user's tenantId, falling back to tag/create. Note: `clinic_setup_status` in the FE's localStorage is a separate, browser-only marker — clear it (`localStorage.clear(); location.reload()`) if the wizard still appears after re-seeding.
- **`MAX_TOOL_TURNS` raised from 4 → 100** (`functions/src/services/orchestrator.ts:36`). At 4, multi-step CRM flows ran out of turns mid-chain — e.g. `web_search` (parse "last thursday this month") → `crm_list_stages` → `crm_list_leads` → `crm_transition_stage` emitted but never dispatched, leaving a blank assistant turn (no card, no text, no error). 100 makes hitting the cap a real-loop signal, not a legitimate-task casualty. Also added a graceful fallback: when the cap *does* fire we now yield a token explaining we hit the limit, so the UI is never silently empty.
- **Validation-error coaching at execute time** (`tech_trax_crm/caseCorrect.ts`). Wraps every write request in `requestWithEnumRetry` so the user never sees raw Joi 4xx output:
  1. **Enum casing typo** (`"high"` for `[High, Medium, Low]`) → `applyCaseCorrection` recursively rewrites the body to canonical casing and retries (up to 3x). Silent — same turn.
  2. **Value not in enum** (`"normal"` for `[low, high]`) → `buildEnumChoiceError` throws a coaching message: *"The 'Priority' field only accepts 'low' or 'high'. Please ask the user which option they'd like and retry."* Handles `must be one of: A, B, C` AND bracketed/parenthesized `[A, B]` `(A, B)` formats.
  3. **Other Joi shapes** (required / email / date / regex / length / type) → `coachJoiError` rewrites the message to a directive the agent reads and naturally relays. Patterns covered: `is required`, `must be a valid email`, `must be a valid date`, `fails to match the required pattern`, `length must be at least N`, `length must be less than or equal to N`, `must be a (number|integer|boolean|string|array|object)`. Network/auth/precondition/5xx errors are explicitly NOT coached so real failures aren't masked.
  4. **Walker is value-based, not field-scoped** — small false-positive risk if an unrelated string in the body happens to match an enum vocabulary term case-insensitively. Acceptable for the demo; can scope to `hint.fieldLabel` later if it surfaces.
  5. Tests in `__tests__/caseCorrect.test.ts` cover all 8 paths (enum parser, walker, retry success, no-match coach, generic Joi coach, skip-on-non-Joi).
- **HITL auto-continuation for multi-step flows — Phase 1 = Option A; Phase 2 (todo-list visibility) deferred (2026-05-09 late EOD).** Surfaced by an Ahmed Ahmed test: user said "create a new lead called Ahmed Ahmed and assign him to me" — agent listed users, asked for missing phone/email, created the lead via HITL, and then **never assigned**. Root cause is structural: when the orchestrator emits `action_approval_required`, it tells the model "Do not call any more tools this turn" and the chat function returns. `confirmPendingAction` is a separate Cloud Function — it executes the write in isolation and writes to `pendingActions/{id}` + `auditLogs/{id}`, but does NOT re-invoke the orchestrator. The agent never sees the executed write and never gets to continue. Compounding this: the executed tool's result is not persisted as a `toolCall` on any assistant message, so even if we did auto-continue, history-replay (Layer 1 from earlier today) would have nothing to show the model. **Decision: build Option A first, evaluate Option B (explicit todo list) only after A is verified in production.** **Phase 1 (Option A) shipped + verified in production 2026-05-09 late EOD** — verified end-to-end via the *"create a new lead called mohamed Ahmed and assign him to me"* test: agent listed users, asked for missing email, created lead via HITL, on confirm the FE auto-fired `[continue]`, agent re-emitted `crm_list_leads` (to find the new lead's id from the verified result) and then `crm_assign_lead` parallel HITL. Both the create AND the assign cards appeared and both confirmed cleanly. Logs showed `[ORCH] cache refreshed: cachedContents/<id>` on each turn — Layer 3 also confirmed working alongside the auto-continue. **`[continue]` is hidden in the chat UI** by a 3-line guard in `ChatMessage.tsx`'s user-message branch: `if (message.content === "[continue]" && !message.attachments?.length) return null` — synthetic user messages aren't user-authored, they're a backend-orchestration signal, so we skip rendering the bubble. **Race fix shipped immediately after first failed test (2026-05-09 23:22):** the first version of `recordExecutedAction` called `sendMessage("[continue]")` inline. But the original chat turn's `setStreaming(false)` lives in a `finally` block that may not have flushed yet at the moment the user clicks Confirm — and `sendMessage`'s first guard is `if (streaming) return`, so the call dropped silently with NO log line. Verified the failure mode against the Ahmed Alaa test: `[ORCH] query="[continue]"` never appeared in emulator logs even though `confirmPendingAction` succeeded and the synthetic `hitl_executed` message was written to Firestore. Fix uses a `pendingContinuationRef` flag set inside `recordExecutedAction` plus a `useEffect` that watches `streaming` — when streaming flips to false (the original turn actually finished), the queued continuation fires. Also tries inline if streaming is already false at call time. Race-safe whether the user clicks Confirm before OR after `setStreaming(false)` flushes. The agent's response to `[continue]` (either further tool calls or a final summary) shows as the next assistant turn — no visual gap. **Note on the agent producing a final summary:** when nothing remains pending after a `[continue]`, the system-prompt section "CONTINUATION AFTER USER APPROVAL" tells the agent to "produce a brief confirmation summary — no further tool calls needed". So the user sees: user prompt → tool cards → HITL confirms → (hidden [continue]) → "Done — Mohamed Ahmed has been created and assigned to you." Final shape went purely FE-driven: final shape went purely FE-driven: (1) when `ActionApprovalCard.handleConfirm` succeeds, it bubbles the result up via a new `onConfirmed(action, result)` callback threaded through `ActionApprovalList` → `ChatMessage` / `StreamingMessage` → `MessageList` → `ChatPanel`. (2) `ChatPanel` calls a new `useChat.recordExecutedAction(action, result)` helper that writes a synthetic assistant message into the messages collection — `{role: "assistant", content: "", agentType: "hitl_executed", toolCalls: [{id: actionId, name, args, status: "done", output: JSON.stringify(result).slice(0, 8000), startedAt}]}` — then fire-and-forget calls `sendMessage("[continue]")`. (3) The orchestrator's history-replay (Layer 1) naturally picks up the new tool call as a `functionCall`/`functionResponse` pair on the next turn, so the agent SEES the executed write and its real result. (4) New system-prompt section "CONTINUATION AFTER USER APPROVAL" teaches the agent that `[continue]` means "look at the just-executed tool result in history and finish any remaining steps from the original request". **Why FE-driven instead of `confirmPendingAction` writing back to the originating message:** avoids a backend round-trip pattern that would have needed a `messageId` round-tripped through the SSE event AND a chained Firestore write inside the callable. The FE already knows the messageId of the message it just wrote, AND already has the action's full `tool`/`args`/`summary` in `PendingActionEvent`, so the synthetic-message approach is simpler and keeps `confirmPendingAction` focused on its one job. **Idempotency note:** the synthetic message uses `action.actionId` as the `toolCall.id`, so even if the user somehow re-confirms, history-replay will only show one entry per actionId. **Out of scope for both phases:** auto-retry on HITL execution failure (cancel/error paths don't auto-continue — only `executed`), batch-emit of all HITL actions in one turn (rejected because subsequent steps depend on prior steps' results — e.g. `crm_assign_lead` needs the leadId from `crm_create_lead`). **Reasons for staging A then B:** (a) A solves the actual bug; B is pure visibility on top. (b) Building both at once bakes in a checklist UX before we know if multi-step "just works" on A alone. (c) If we later add B, the right shape is a *declarative* `agent_plan({steps})` tool the agent emits ONCE up front, with the UI checklist derived from observed `toolCalls[]` matches — NOT a parallel state machine the agent has to keep in sync.
- **CONTINUATION-prompt classifier — verified end-to-end (2026-05-10).** Bassiouny Bassiouny test run after the classifier prompt change: user prompt batched create + assign + transition + update_lead with a natural-language date ("next monday"). Trace: T2 emits 3 reads + create_lead → preflight caught a malformed phone → ask_user → user clarified → create HITL → confirm → `[continue]` → T7 emits all three remaining writes in logical order (assign, transition, update) as a single HITL batch → confirm assign → `[continue]` → **T10 produced text only** ("Lead has been assigned. The remaining actions … are currently awaiting your approval"), zero tool emissions → confirm transition → `[continue]` → **T13 again text only** ("The updates are pending your approval"), zero tool emissions → confirm update → `[continue]` → final consolidated summary listing all four actions. Net: 3 HITL confirms for 4 actions, no duplicate cards, no `[continue]` leakage, no premature-success claims. The DONE / AWAITING / STALE / NOT YET DONE classifier prompt rule is doing exactly what was intended — giving the model a structured response shape to reason against rather than relying on a soft negative imperative. Screenshot saved to user's session for reference.
- **CONTINUATION-prompt classifier for batched HITL (2026-05-10).** The Bassiouny Hazem test (verified via Firestore REST + DevTools MCP) revealed that even with the dedup fix and the prompt rule "DO NOT re-emit awaiting actions", the model still re-emitted both `assign + transition` on the `[continue]` turn after assign-confirm. Tracing through: my history-replay correctly dropped the T5 `assign` proposal (matched by T6's synthetic execution) and kept T5's `transition` proposal as `{status: "awaiting_user_approval"}`. Despite that, the model produced T8 with FRESH HITL cards for both. The previous prompt phrasing was too implicit — "complete REMAINING steps from the original request" gave the model permission to consider transition "remaining" without explicitly distinguishing "still-pending-from-earlier" from "not-yet-attempted". **Fix:** added a CRITICAL classifier step to the CONTINUATION AFTER USER APPROVAL section. Before emitting any tool on a `[continue]` turn, the model is instructed to classify EACH step of the original multi-step request into one of four buckets by scanning history: (1) **DONE** — real `summary` data in history, do not re-emit; (2) **AWAITING** — `{status: "awaiting_user_approval", ...}` stub in history, do not re-emit because the user is deciding on the existing card; (3) **STALE** — re-fetch; (4) **NOT YET DONE** — only this bucket warrants a fresh emission. Plus an explicit "most common mistake" callout: re-emitting an AWAITING step as if it were NOT YET DONE produces duplicate approval cards. **Why a structured classifier vs. a one-liner:** the model is good at following step-by-step instructions tied to observable signals (the actual response shapes in history) but fuzzy on negative imperatives like "don't re-emit". Forcing classification before action makes the rule load-bearing instead of advisory. **Out of scope this session:** orchestrator-side hard guardrail that drops a model-emitted tool call when an awaiting/done one with matching name+args already exists. We considered it but the prompt fix is reversible and lower-risk; if the Bassiouny pattern recurs after this prompt change, that's the next escalation. (The previous dedup fix that drops awaitingApproval entries from history-replay when superseded already works as designed — verified by tracing canonArgs through the Firestore message data.)
- **`[continue]` leakage + premature-success hallucination (2026-05-10).** Two related model-side bugs surfaced by the Hazem Bassiouny test, both verified by inspecting the messages collection via Firestore emulator REST + Chrome DevTools MCP: **(1) `[continue]` rendered as a chat bubble** — a persisted assistant message had `role: "assistant", content: "[continue]"` (the model literally emitted the system-prompt continuation marker as its own text alongside the legitimate first-turn tool calls). The existing FE guard at `ChatMessage.tsx` only checked `role === user`, so the assistant version slipped through and rendered between tool cards. **(2) Premature success** — on the `[continue]` turn after the user confirmed `crm_assign_lead`, the agent saw `transition_stage` replayed as `{status: "awaiting_user_approval"}` (correctly — my history-replay refinement is doing its job) but produced text *"Hazem Bassiouny has been successfully created, assigned to you, and moved to the **Snooze** stage..."* — claiming the still-pending transition was done. The user saw a "we did it" summary with the transition card still asking for confirmation underneath it. **Fix (a) — FE scrub:** new `scrubContinueMarkers(content)` helper in `ChatMessage.tsx` strips whole-line `[continue]` occurrences from rendered assistant content (whole-line only, never substrings inside legitimate text) and `renderContentWithCitations` short-circuits if scrubbed content is empty. Defense-in-depth on top of the prompt rule. **Fix (b) — prompt:** the awaiting_user_approval rule now also forbids past-tense success claims about pending actions — *"DO NOT in your text response claim that action succeeded, was completed, or use past-tense success language about it ('has been moved', 'is now assigned', 'has been transitioned'). The user is still deciding. Only describe an action as done when its tool result in history carries the actual server response (a `summary` field with real data), NOT a `{status: "awaiting_user_approval", ...}` stub."* Plus an explicit no-emit rule: *"NEVER emit the literal string `[continue]` in your text response under any circumstances. If you have nothing useful to say in a turn, output nothing."* **Why both layers:** the prompt is the structural fix (the model should never produce that text), the FE scrub is the safety net for when it does anyway. Without the prompt fix, the same hallucination would happen on every multi-step HITL flow; without the scrub, future prompt regressions would re-leak the marker.
- **HITL replay refinement — superseded-vs-pending distinction (2026-05-10).** The original dedup fix (see entry below) unconditionally dropped every `awaitingApproval` toolCall from history-replay. That worked when there was a 1:1 match between proposal and synthetic execution, but broke when a turn batched MULTIPLE HITL writes and the user confirmed only some of them before `[continue]` fired. Repro: "create lead Hazem then assign him to me then transition to snooze with custom fields" — turn B emitted `assign + transition` in parallel; user confirmed assign; `[continue]` fired; history-replay stripped BOTH proposals; only the assign synthetic existed; agent had no record of ever emitting `transition` and re-emitted it (creating a redundant approval gate the user had to confirm a second time). **Fix:** replay now distinguishes superseded proposals (drop) from still-pending proposals (keep with stub). New `replaySlotsByTurn` builds a per-turn list of `{tc, awaitingStub}` slots: an `awaitingApproval` entry is dropped when a later non-awaiting toolCall with matching `name + canonicalArgs` exists anywhere in history (the synthetic `hitl_executed` message that supersedes it); otherwise the entry is kept and replayed with `functionResponse: {status: "awaiting_user_approval", reason: "...do NOT re-emit..."}`. Args matching uses sorted-key `JSON.stringify` since both proposal and synthetic round-trip through the pendingAction doc with identical args. The forward write-collection pass and the replay pass both iterate `replaySlotsByTurn` so write indices align for `staleReason` lookups (writes still skip `awaitingStub` slots since pending proposals haven't actually written upstream). New REQUIREMENTS GATHERING line: *"If a prior tool result shows `{status: 'awaiting_user_approval', ...}` it means you already proposed that HITL-gated write and the user has not yet confirmed or cancelled it. DO NOT re-emit it."* — defense-in-depth on top of the structural fix. **Why match by name+args instead of id:** the proposal toolCall.id is the orchestrator's `eventId`, the synthetic uses `actionId`; threading actionId through the proposal would have rippled across more layers. name+args is deterministic enough because identical writes within one session are extraordinarily rare and the worst-case (a repeated identical write being silently deduped) is a small, recoverable degradation vs. the current bug of forced redundant confirmations.
- **HITL approval serialization + batching rules (2026-05-09 late EOD).** Two coordinated changes that fix a related-but-distinct issue from the dedup fix below: when the agent emits multiple HITL-gated writes in one turn, the user used to see all approval modals stacked simultaneously and could click through them while the first was still executing — leading to confusion AND, when later actions had args dependent on earlier ones (e.g. `crm_assign_lead({leadId})` next to `crm_create_lead` for a new lead), the dependent action's args were fabricated/junk. **(a) FE serialization** — `ActionApprovalList` (`ActionApprovalCard.tsx:379`) now renders all *resolved* cards (so the user keeps a record of what they confirmed) plus the *first still-pending* card; subsequent pending cards stay hidden until the prior resolves. The list subscribes to a new `useActionStoreTick()` hook in `connectorActionStore.ts` (a counter incremented on every `notify()`) so it re-renders and unhides the next card automatically when any card resolves. New `isActionResolved(actionId)` helper centralizes the "executed/cancelled/expired/error" check. **(b) Prompt rules** — REQUIREMENTS GATHERING in `config.ts` rewritten from "emit ALL writes in a single turn" to two explicit sub-rules: **DEPENDENCIES** (never batch a HITL write whose args would depend on a sibling write's outcome — emit the first, wait for `[continue]`, then emit the next) and **LOGICAL ORDER** (when batching mutually-independent HITL writes, order them in the sequence a human would naturally execute them — assign before transition, parent before child — because the user sees the approval cards one at a time in array order). **Why two layers (UX + prompt) instead of one:** the FE serialization is a defense-in-depth safety net even when the prompt is ignored — the user is never asked to think about more than one card at a time. The prompt fix addresses the deeper data-correctness angle (fabricated args) which serialization alone can't solve since the args are baked at dispatch time. Out of scope: orchestrator-side queueing of pending actions (would require holding back `action_approval_required` events server-side and re-firing on confirm — significantly more complex than the FE-only approach).
- **HITL proposal vs. executed result — replay-side dedup (2026-05-09 late EOD).** Symptom: after a HITL write actually succeeded (e.g. `crm_assign_lead`), the very next `[continue]` turn re-emitted the *same* write as a fresh pending action, forcing the user to confirm again. Loop terminated only after the second redundant confirm. Root cause was a shape mismatch between what gets persisted on the originating assistant turn versus what gets persisted on the synthetic `hitl_executed` follow-up message: the orchestrator's `tool_call` event for a `pendingAction` result (`orchestrator.ts:688-700`) yielded `status: "done", output: res.pendingAction.summary` — i.e. a human-readable *preview* string — and the FE persisted that on the originating message. Then `confirmPendingAction` ran and `recordExecutedAction` wrote a *second* assistant message (`agentType: "hitl_executed"`) with its own toolCall using `actionId` as the id and the real server result as `output`. On `[continue]`, `reconstructHistory` walked both messages and replayed each as its own `functionCall` + `functionResponse` pair — so Gemini saw two `crm_assign_lead` calls with different ids, neither response carrying explicit "this write has landed" semantics; combined with the "CONTINUATION AFTER USER APPROVAL — finish remaining steps" prompt, the model resolved the ambiguity by re-emitting. **Fix:** new `awaitingApproval?: boolean` flag on `ToolCallEvent` / `PriorToolCall` (BE) and `ToolCall` (FE). The orchestrator sets it on the `tool_call` event when `res.pendingAction` is present (and only then). `reconstructHistory` filters out toolCalls with `awaitingApproval === true` in BOTH passes — the forward write-collection pass (so HITL proposals never invalidate later reads) AND the replay pass (so the model never sees the proposal as a `functionCall`/`functionResponse` pair). Indices stay consistent because both passes iterate the same pre-filtered `filteredTcsByTurn[t]` array. The synthetic `hitl_executed` message is unaffected — its toolCall has no `awaitingApproval` flag, so it replays normally as the single source-of-truth call/response pair for that logical action. **Why this shape over alternatives:** (a) matching pending↔executed by `actionId` would have required threading the id through more layers; (b) skipping unconditionally is safe because if the user *cancels* (or lets it expire) there's also no synthetic follow-up — losing the "I tried" signal is acceptable since the agent's next attempt will naturally re-emit if needed; (c) the FE's `ActionApprovalCard` is driven by the separate `action_approval_required` event, NOT the `tool_call` event, so the card UX is untouched; the only visible change is that a small "tool ran ✓" pill no longer appears on the originating message for HITL-gated writes — which is correct, because the write hadn't actually run yet. Verified `tsc` clean both sides.
- **History replay + write-aware stripping + Gemini context caching (2026-05-09).** Three coordinated changes that together make the agent stop re-fetching data it already knows AND make the conversation prefix cheap to send. Triggered by logs showing "assign him to me" prompts emitting redundant `crm_list_users` + `crm_list_leads` even though the previous turn had just listed both. Root cause: orchestrator's history reconstruction at `orchestrator.ts:159–165` stripped everything but `text`, so the model saw its paraphrase ("I moved Adel...") but not the structured ids it had fetched. **Layer 1 (replay):** new `reconstructHistory(history)` walks each prior assistant turn and re-emits its persisted `toolCalls[]` as matched `functionCall` + `functionResponse` parts (one model turn carrying every call, one user turn carrying every response — same shape as live turns). FE plumbs `toolCalls?` through `ChatParams.history`; BE-local `PriorToolCall` type avoids cross-importing FE types. **Layer 2 (write-aware strip):** new `connectors/invalidations.ts` declares the write→read map (e.g. `crm_update_lead({leadId: X})` invalidates any later `crm_get_lead({leadId: X})` AND any `crm_list_leads(*)`; stage/custom-field writes are global). Two-pass: forward pass collects writes, replay pass checks `staleReason()` per call. Stripped reads keep their `functionCall` slot but get a stub `{status: "stale", reason: "invalidated by later <toolName>"}` response (Gemini requires matched call/response pairs; the stub is also a strong signal to re-fetch). Built-ins (`web_search`, `maps_search`, `url_fetch`) intentionally absent from the map — they don't mutate. **Decision: writes only invalidate strictly-later reads** — same-turn writes don't invalidate each other (they were both intentional concurrent mutations). **Layer 3 (Gemini caching, always on):** `client.caches.create({systemInstruction, tools, toolConfig, ttl: "3600s"})` per session, cache resource name + sha256-hash + expiresAt persisted to `sessionDoc.geminiCache`. Subsequent turns gate on `expiresAt > now + 60s` AND `hash === currentHash`, then call `generateContentStream({cachedContent: name})` *omitting* the cached fields (per `@google/genai` 1.46 API). Hash is `sha256(model + systemInstruction + JSON.stringify(decls.map({name, description.slice(0,100)})))` — truncating descriptions stops trivial wording tweaks from blowing the cache. **Decision: no env flag** — caching is always-on with graceful fallback at every layer (cache too small → `INVALID_ARGUMENT` → log + skip; cache stale mid-call → catch, recreate, fall through to non-cached path; no cache yet → normal path then create at end-of-turn). **Decision: one cache event per chat turn**, not per generation call — `caches.create`/`update` happen at the end of `orchestrate()` after the multi-tool loop completes. **Decision: cache write-back is fire-and-forget Firestore update** in the `cache_event` SSE handler — race window if two requests start concurrently on the same session, last-write-wins; acceptable for single-user app. **System prompt addition:** "HISTORY AWARENESS — Trust prior tool results unless a write to the same resource happened since; if a result shows `{status: "stale", ...}`, re-fetch." Plus a side-fix: `limit=200` → `100` on `/api/platform/custom-fields` and `/api/platform/teams/users/*` (backend caps at 100, was returning 422 and silently emptying the custom-fields half of the schema cache). **Verified end-to-end in the local emulator (2026-05-09 evening):** Layer 1 confirmed by the *"list leads…"* → *"assign Yahia to me"* sequence — the second turn went straight to a single `crm_assign_lead` call with zero redundant `crm_list_*` lookups, model latency dropped from ~16s to ~8s. Layer 3 confirmed by inspecting the active session doc in Firestore: `geminiCache: { name: "cachedContents/ibb0...", hash: "6c21...", expiresAt: <epoch> }` populated and `updatedAt` bumped on every chat turn — meaning `caches.create`/`caches.update` succeeded silently each time. **Visibility patch added the same evening:** the success branches of the cache lifecycle in `orchestrator.ts` now log `[ORCH] cache created: cachedContents/<id> (model=..., tokens=..., expiresAt=...)` on first turn of a session and `[ORCH] cache refreshed: cachedContents/<id> (expiresAt=...)` on every reuse, so future audits don't require Firestore-spelunking. The failure path was already logging `[ORCH] cache lifecycle skipped: <reason>`. Plus a defensive `[ORCH] cache create returned no name — skipping persistence` line in case Gemini ever ACKs a create with an empty resource name.
- **`ask_user` clarification flow + REQUIREMENTS GATHERING prompt (2026-05-09).** New orchestrator-level control function the agent emits to batch every clarifying question into ONE structured ask before doing any work. Solves two problems at once: (a) data hygiene — agent stops fabricating field names, dates, user ids when the user under-specifies; (b) multi-action friction — instead of discovering missing data action-by-action via `ValidationPendingError` (one tool at a time), the agent gathers everything up front then runs the full multi-action plan in one autonomous burst. Wire-up: declared at `orchestrator.ts:ASK_USER_DECL` (NOT in connectors — it's always available); the orchestrator special-cases the call AFTER extraction but BEFORE dispatch — emits a `clarification_required` SSE event, halts the turn (no `functionResponse`, no sibling-tool dispatch even if model emitted ask_user alongside writes — running writes while asking for missing data defeats the purpose), and the model picks up the user's answers in the next turn from preserved history. New SSE event handled in `index.ts:540-ish` and FE `streaming.ts`. **Decision: max 1 turn per ask** — `ask_user` cannot fire mid-tool-loop after writes; the agent must ask FIRST or not at all in a given turn (enforced by halting the loop). New components: `lib/clarificationStore.ts` (mirror of `connectorActionStore`), `features/chat/components/ClarificationCard.tsx` (form with text/date/select inputs), `ClarificationRecord` type on `Message`. **Decision: answers fold into a synthesized natural-language follow-up** ("My answers:\n- key: value\n...") sent via `sendMessage()` — no special continuation API needed, agent reads it as a normal user message with full history. Cards persist on the assistant message doc so they survive reload (resolution state is in-memory only — once answered the card collapses to "Answered ✓"; the synthesized user message is the source of truth for what was sent). Plus: prompt addendum "REQUIREMENTS GATHERING" appended to `WEB_TOOLS_PROMPT_ADDON_V2` teaching the gather-first pattern + lookup-tool-first hierarchy (use `crm_list_users` / `crm_list_stages` / `crm_list_custom_fields` BEFORE asking the user about anything lookup-able). Plus: tightened `preflightAssignLead` (rejects non-ObjectId-shape `assigneeUserId` to catch agents that fabricate ids from names) and `preflightTransitionStage` (rejects `toStageId` not in this tenant's pipeline, lists real stages). Defense in depth — even if the model ignores the prompt, the preflights coach it back to `crm_list_*` + `ask_user` instead of letting fabricated values reach the HITL approval card. **Out of scope (deferred):** LLM-based "checker sub-agent" pass, batched single-card HITL approval UX (user picked stacked-cards), per-tenant defaults config.
- **Self-verifying write handlers (2026-05-09).** Every write handler now asserts the response body actually reflects the intended mutation, instead of trusting "200 OK". Backed by a `verify(ok, message, details)` helper that throws `code: "verification_failed"` on mismatch — so the agent's summary can't lie about what happened (`Mongo` `__v` bumps, partial server merges, idempotency middleware short-circuits, "soft success" responses are all real failure modes that used to slip through). Each handler checks the smallest meaningful invariant: assign → `lead.crm.assignedTo === resolvedAssignee`; transition → `lead.crm.stageId === toStageId`; update → every key sent in the body (top-level, `crm.*`, `customFields[]` matched on fieldId) round-trips with loose equality (`String(a) === String(b)` to handle ObjectIds/Dates/enums); create lead → `_id` exists + `firstName`/`lastName`/`email` echo back; create/update stage → name + color + sorted mandatoryFields list; create custom-field → fieldName + fieldType; update custom-field → label/options/flags; delete stage → re-fetch the stages list and confirm the id is gone. **Decision: layer 1 only for now** (per the "three layers" plan: self-verifying handlers, structured what-I-did log, optional checker sub-agent). LLM-based checker explicitly skipped — the latency tax on every multi-step flow isn't worth it until layer 1 has had time to surface what kinds of mismatches actually occur in practice. **Equality is intentionally loose** because backend sometimes stringifies dates and ObjectIds inconsistently across endpoints; tighter checks generate false positives. Helpers: `eqIdLike(a, b)` (string-coerced equality, null-safe), `pickId(v)` (handles populated `{_id}` vs raw id strings).
- **User resolution for "assign to me" / by name (2026-05-09).** New `crm_list_users` read-only tool returns the assignable workspace members the CRM FE shows in its assign popover (managers + team leads + unassigned-pool, deduped) shaped as `{id, name, email, role, isMe}`. Composes from three `/api/platform/teams/users/{managers,team-leads}` + `/api/platform/teams/unassigned/members` calls in parallel with `Promise.allSettled` so a single 4xx doesn't blank the list. The connected user's identity is decoded from the access-token JWT payload via new `TechTraxClient.getIdentity()` (uses `payload.userId ?? payload.sub`) — this avoids a `/users/profile-data` round-trip that 403s for sales-manager-tier roles. The connected user is always added to the result set with `isMe: true`, even if they don't appear in any team list (admin edge case). `crm_assign_lead` now accepts `assigneeUserId: "me"` (and aliases `myself`/`self`/`i`/`current_user`) and resolves them in `preflightAssignLead` *before* the HITL approval card is rendered, so the card preview shows the resolved id rather than the opaque shortcut. **Decision:** resolution is a string-replace, not a separate tool call — keeps the agent-side flow short for the common "assign to me" prompt while still requiring `crm_list_users` for ambiguous names. Total connector tools now: **15**.
- **Concurrent HITL writes on the same lead — auto-retry on 412 (2026-05-09).** When the agent emits multiple write tools in parallel (e.g. `crm_assign_lead` + `crm_transition_stage` from one user prompt), each becomes its own pending action with its own preflight-time `lockToken`. The user approves them sequentially; the second action's stored token is now stale because the first action just bumped `Last-Modified`, even though `refreshLockToken` runs at handler start (there's still a tiny window between refresh and write where a sibling can land). New `executeWithLockRetry(c, ctx, leadId, mutate)` helper wraps every write that uses `ifUnmodifiedSince` (`handleUpdateLead` PATCH, `handleTransitionStage` PATCH + transition POST, `handleAssignLead` POST). On `code: "precondition_failed"` it does `refreshLockToken` and retries the same call exactly once; any other error bubbles unchanged. **Decision:** retry max 1, not N — if the second attempt also 412s there's a real conflict (likely a human edit in the CRM FE while the agent was working) and the user should retry the whole flow rather than us looping. The semantic of auto-retry is "the agent's intent is to apply this change to the latest version of the lead, regardless of order with sibling actions" — which matches how multi-tool prompts are written. We're not hiding human conflicts from a human editor; we're hiding sibling-tool ordering from the agent's own batch.
- **Custom field routing — full catalog, not just mandatory-from-stage (2026-05-09).** Original `schemaCache.customFieldNameToId` only contained custom fields referenced as `mandatoryFields` on some stage, because the stages endpoint was the only payload we read. Hit a real bug: agent tried `crm_update_lead` with `close_date` (a non-mandatory custom field), routing dropped it as unrouted, the resulting empty `{}` PATCH was rejected by Joi (`"value" must have at least 1 key`) AFTER the user had already approved the HITL card. Fixes: (1) `getSchema` now also calls `GET /api/platform/custom-fields?module=crm&isActive=true&limit=200` and merges every (`fieldName` → `fieldId`) into the route map, plus stores a `customFields[]` summary for coaching messages — degrades gracefully (warns, falls back to mandatory-only) if that endpoint 4xxs; (2) cache path bumped `_schema/v1` → `_schema/v2` so old-shape docs aren't returned (no migration code needed — the next call repopulates); (3) `preflightUpdateLead` now pre-routes the agent's `fields` and throws `ValidationPendingError` with the full known-field catalog when *every* key is unrouted, so the user is never asked to approve a doomed card; (4) `handleUpdateLead` keeps a defense-in-depth check at execute time — if the patch body is somehow still empty, it throws a structured `validation_error` with the same coaching list instead of letting the empty body hit the backend. Same `buildKnownFieldList(schema)` helper feeds both messages so the agent always sees: `top-level: ... | crm subdoc: ... | custom: fieldName ("Label"), ...`.
- **Pipeline configuration tools (2026-05-09).** Added 7 tools so the agent can manage the CRM's structure, not just its records: stages (`crm_create_stage` POST `/api/crm/stages`, `crm_update_stage` PATCH `/api/crm/stages/:id`, `crm_delete_stage` DELETE with optional `reassignToStageId` body for stages that hold open leads) and custom fields (`crm_list_custom_fields` GET `/api/platform/custom-fields?module=crm`, `crm_create_custom_field` POST, `crm_update_custom_field` PUT — note PUT not PATCH — `crm_delete_custom_field` DELETE). All writes go through HITL approval and the same `requestWithEnumRetry` coaching layer. Each mutation calls `invalidateSchema(uid)` (new helper in `schemaCache.ts`) so the next `crm_list_stages` / lead transition sees the new pipeline shape instead of the cached one. **Decision:** stage `mandatoryFields[]` and custom-field `options[]` updates are *full replacements*, not patches — the backend Joi schemas don't support add/remove deltas, so the agent must list current values first and send the merged set. Preflights enforce stage hex-color regex, field-type/location enum, and the "options[] required for select fields" rule client-side so the user sees a coaching message instead of a 400. **Client `method` union widened to include `PUT`** in `client.ts` (custom-field updates use PUT per backend convention).

## Connectors (Phase 6 — `feat/connectors-google-calendar`)

User-authorized third-party integrations callable from chat. First connector: Google Calendar.

### Architecture

- **Generic framework** at `functions/src/services/connectors/`:
  - `registry.ts` — singleton provider registry; `dispatch(toolName, args, ctx)` for read tools (immediate exec) and write tools (HITL gate via `pendingActions`).
  - `types.ts` — `ConnectorProvider`, `ConnectorTool`, `ConnectorContext`, `EncryptedBlob`, etc.
  - `crypto.ts` — AES-256-GCM with random per-record DEK, DEK wrapped via Cloud KMS (`@google-cloud/kms`). Falls back to DEV MODE (base64) when `CONNECTOR_KMS_KEY` is unset (local emulator only).
  - `stateJwt.ts` — HS256 sign/verify for OAuth state CSRF protection. 10-minute TTL.
  - `oauth.ts` — `buildOAuthStartUrl` and `handleOAuthCallback`. Encrypts refresh + access tokens before persisting. Supports incremental authorization via `mode: 'initial' | 'expand'`.
  - `pendingActions.ts` — propose/confirm/cancel helpers backed by `pendingActions/{id}` collection (5-minute default TTL).
  - `audit.ts` — `writeAudit` to `auditLogs/{id}` with 90-day TTL field.
  - `index.ts` — `bootConnectors()` (idempotent, gated on `CONNECTORS_ENABLED=true`); registers `googleCalendarProvider`.
- **Google Calendar provider** at `functions/src/services/connectors/google_calendar/`:
  - 6 tools: `gcal_freebusy`, `gcal_list_events` (read; no approval), `gcal_create_event`, `gcal_update_event`, `gcal_delete_event`, `gcal_respond_to_event` (write; HITL gate).
  - Idempotent event IDs derived from `ctx.idempotencyKey` (registry computes a SHA-256 of `{uid, sessionId, tool, canonical(args)}` and threads it through).
  - Read-before-write on update/delete/RSVP: `events.get` first to validate existence + capture state for audit.
  - Initial scopes (read): `openid email profile calendar.readonly calendar.freebusy`. Full scopes (read+write): adds `calendar.events`. Frontend can request `mode=expand` for the writeable scope on demand.
- **Per-user OAuth tokens** at `users/{uid}/connectors/{provider}` — refresh tokens encrypted via Cloud KMS envelope; server-only via Firestore rules.
- **HITL approval gate**: write tools emit an `action_approval_required` SSE event from `/chat`; the `ActionApprovalCard` in chat renders a Confirm/Cancel UI with a 5-minute countdown; on Confirm, frontend calls `confirmPendingAction({actionId})` callable which validates ownership/status/expiry then runs the handler with the same idempotency key.
- **Audit log** at `auditLogs/{id}` covers every connector tool execution (read or write, success or error) with `{uid, sessionId, provider, tool, args, result, status, idempotencyKey, latencyMs, model, reasonCode, createdAt, ttlAt}`.
- **Incremental authorization (implemented)**: first connect requests read-only scopes (`calendar.readonly`, `calendar.freebusy`). When the agent calls a write tool and the user hasn't granted `calendar.events`, the registry returns `{kind:'scope_required', missingScopes}` (no throw). The orchestrator yields a `scope_expansion_required` SSE event AND tells Gemini to inform the user. The frontend renders a `ScopeExpansionCard` in the chat with a "Grant access" button that opens the OAuth popup with `mode=expand`. After consent, the user re-prompts and the write proceeds via the normal HITL approval flow.
- **MCP forward-compatibility**: `ConnectorTool.declaration` is JSON-Schema-shaped (matches MCP `CallToolRequest`); we can re-expose providers as MCP servers later without rewriting handlers.

### Frontend
- `/settings/connectors` page (`src/features/settings/components/ConnectorsPage.tsx`) — list of providers with Connect/Disconnect/Reconnect.
- `useConnectors` hook opens OAuth in a popup, listens for `postMessage` from the callback page.
- `ActionApprovalCard` (`src/features/chat/components/ActionApprovalCard.tsx`) and `ScopeExpansionCard` render inline under the assistant message that triggered them. They are **anchored to their turn**: when streaming finishes, the events are persisted onto `message.pendingActions` and `message.scopeExpansions` on the assistant message doc, so they stay attached to that turn (no pile-up on subsequent turns).
- Live resolution state lives in a module-level store at `src/lib/connectorActionStore.ts` (subscribed via `useSyncExternalStore`). This survives the StreamingMessage → ChatMessage unmount/remount boundary, so a card the user just confirmed still shows "Done ✓" when its hosting message re-renders from Firestore.
- Cards are fully collapsible after resolution (matching `ToolCallCard`): pending = full card with countdown + Confirm/Cancel; resolved = one-line pill with status icon, click to expand args + result.
- After page reload the runtime store is empty; cards fall back to their lifecycle (pending if `expiresAt > now`, otherwise expired). We never fake an "executed" state from persisted records — the backend `auditLogs` collection is the source of truth for what actually happened.
- `streaming.ts` handles `action_approval_required` and `scope_expansion_required` SSE events; `useChat` accumulates them per-turn and clears on `sendMessage`; `ChatMessage` renders persisted records, `StreamingMessage` renders live ones.

### Cloud Functions exports
- `connectorOAuthStart` (HTTP, requires Firebase ID token) — returns `{ url }` to the Google consent screen.
- `connectorOAuthCallback` (HTTP, public) — exchanges code, encrypts + persists tokens, posts back to opener and closes.
- `getConnectorStatus` (callable) — `{connectors: ConnectorStatus[]}` for all registered providers.
- `disconnectConnector({provider})` (callable) — best-effort `oauth2.revokeToken`, marks doc `revoked`, deletes encrypted token blobs.
- `confirmPendingAction({actionId})` (callable) — auth-gated: ownership + status + expiry checks, then `executeApprovedAction`.
- `cancelPendingAction({actionId})` (callable) — same gating, marks `cancelled` + audits.

### Firestore rules (`firestore.rules`)
Server-only deny rules added for:
- `users/{uid}/connectors/{provider}` (refresh tokens live here, even though encrypted)
- `pendingActions/{actionId}`
- `auditLogs/{logId}`

### Provisioning status (staging) — done

For `the-knowledge-base-staging`, project number `105713297964`:
- ✅ Cloud KMS API + Calendar API enabled.
- ✅ KMS keyring `connectors` (us-central1) + key `refresh-tokens` (symmetric, software protection).
- ✅ IAM: compute SA granted `roles/cloudkms.cryptoKeyEncrypterDecrypter` on the key.
- ✅ `CONNECTOR_STATE_SIGNING_SECRET`, `CONNECTOR_KMS_KEY`, `GOOGLE_OAUTH_*` all in `functions/.env.the-knowledge-base-staging`.
- ✅ OAuth web client created in GCP; consent screen configured with all 6 scopes; test users added.
- ✅ Firestore rules deployed (deny-all on `users/{uid}/connectors/**`, `pendingActions/**`, `auditLogs/**`).
- ✅ Functions deployed: `chat`, `connectorOAuthStart`, `connectorOAuthCallback`, `getConnectorStatus`, `disconnectConnector`, `confirmPendingAction`, `cancelPendingAction`.
- ✅ `CONNECTORS_ENABLED=true` on staging.
- ✅ End-to-end verified: connect, scope expansion, freebusy/list_events reads, create_event/update_event/delete_event with HITL approval, audit log writes.

### Production rollout (when ready)

Repeat the staging steps for `the-knowledge-base-82d72`:
- New OAuth web client + consent screen on the prod GCP project (separate redirect URI: `https://us-central1-the-knowledge-base-82d72.cloudfunctions.net/connectorOAuthCallback`).
- New KMS keyring + key + IAM binding on prod.
- Generate fresh `CONNECTOR_STATE_SIGNING_SECRET` for prod env.
- **Complete Google's OAuth app verification** before going past 100 test users — `calendar` scopes are sensitive. Plan 1–4 weeks.
- `CONNECTORS_ENABLED=false` on prod until verification is done; users on prod see no connector UI.
- Firestore rules + functions deploy from `main` (after merge from `feat/connectors-google-calendar`).

## Full Requirements

See `KB-MVP-PRD.md` for complete PRD.
